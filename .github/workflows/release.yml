name: App release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      content: release
    secrets: inherit

  publish:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Collect
        run: |
          set -euo pipefail
          mkdir -p release
          cp "$(find artifacts/android -name '*.apk' | head -n1)" release/sevenreadings-android.apk
          cp "$(find artifacts/android -name '*.aab' | head -n1)" release/sevenreadings-android.aab
          cp artifacts/linux-x64/*.tar.gz artifacts/linux-arm64/*.tar.gz release/
          cp artifacts/windows-x64/*.zip artifacts/macos/*.zip artifacts/ios-unsigned/*.zip release/
          (cd artifacts/web && zip -qr ../../release/sevenreadings-web.zip .)
          (cd release && sha256sum -- * > SHA256SUMS)
          ls -la release
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: release/*
