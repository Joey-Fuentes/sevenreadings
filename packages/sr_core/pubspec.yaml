name: sr_core
description: Pure-Dart domain model for sevenreadings - canon, verse references, versification.
publish_to: none
version: 0.1.0
resolution: workspace
environment:
  sdk: ^3.6.0
dependencies:
  meta: ^1.15.0
dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
