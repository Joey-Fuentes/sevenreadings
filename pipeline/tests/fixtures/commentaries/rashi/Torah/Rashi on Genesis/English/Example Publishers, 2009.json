{
 "language": "en",
 "title": "Rashi on Genesis",
 "versionSource": "https://example.org",
 "versionTitle": "Example Publishers, 2009",
 "license": "CC-BY-NC",
 "actualLanguage": "en",
 "text": [
  [
   [
    "A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. A restricted translation that must never be shipped, however large it is. "
   ]
  ]
 ]
}
